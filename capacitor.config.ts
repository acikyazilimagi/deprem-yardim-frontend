import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.depremyardim.app",
  appName: "Deprem Yardim",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
